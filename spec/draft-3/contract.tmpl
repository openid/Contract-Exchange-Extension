<t>
A CX Contract is the container JSON object of CX Acceptances and enveloped by  {{ xref.JSON_SIMPLE_SIGN_1_0 }}.
Every party is provided its own contract which share the same contract identifier.
</t>

<t>
Following parameters are placed in a JSON object held by "payload" parameter  of {{ xref.JSON_SIMPLE_SIGN_1_0 }} JSON.
</t>

<t>
    <list style="symbols">
        {% for p in contract: %}
            <t> {{p.name}}
                <list style="symbols">
                    {% for v in p.note: %}
                        <t>{{v}}</t>
                    {% endfor %}
                </list>
            </t>
        {% endfor %}
    </list>
</t>

<t>The following is a non-normative sample for CX Contract JSON object:</t>

<figure>
    <artwork><![CDATA[
{% include "contract.json" %}
    ]]></artwork>
</figure>
